{
  "tag": "row-no-divider",
  "category": "row-structure",
  "title": "Whitespace-only separation",
  "description": "Removes the hairline divider entirely and separates rows purely with extra vertical padding, relying on whitespace rhythm rather than any rule or fill to group the list.",
  "baseline_ref": ".arrival-row (border-bottom: 1px solid var(--color-border); padding: 11px 0)",
  "changes": [
    {
      "file": "app/assets/css/main.css",
      "kind": "css",
      "selector": ".arrival-row",
      "intent": "Drop the divider and compensate with more vertical padding so rows still read as distinct groups.",
      "declarations": { "border-bottom": "none", "padding": "18px 0" }
    }
  ],
  "composes": []
}
